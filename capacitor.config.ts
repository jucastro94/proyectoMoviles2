import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mobilesappsii.app',
  appName: 'gestion-perfecta',
  webDir: 'dist/proyecto-app-angular',
  server: {
    androidScheme: 'https'
  }
};

export default config;
